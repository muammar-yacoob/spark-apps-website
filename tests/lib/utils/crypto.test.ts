export {};
const { generateApiKey, generateAppIdFromName } = require('../../../lib/utils/crypto');

describe('generateApiKey', () => {
  it('should work', () => {
    // TODO: add assertions for generateApiKey
  });
});

describe('generateAppIdFromName', () => {
  it('should work', () => {
    // TODO: add assertions for generateAppIdFromName
  });
});
