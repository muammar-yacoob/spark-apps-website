export {};
const {
  getExchangeRate,
  detectCurrencyFromIp,
  detectCurrencyFromLocale,
  getClientIp,
  detectViewerCurrency,
  formatAmount,
} = require('../../../lib/utils/currency');

describe('getExchangeRate', () => {
  it('should work', () => {
    // TODO: add assertions for getExchangeRate
  });
});

describe('detectCurrencyFromIp', () => {
  it('should work', () => {
    // TODO: add assertions for detectCurrencyFromIp
  });
});

describe('detectCurrencyFromLocale', () => {
  it('should work', () => {
    // TODO: add assertions for detectCurrencyFromLocale
  });
});

describe('getClientIp', () => {
  it('should work', () => {
    // TODO: add assertions for getClientIp
  });
});

describe('detectViewerCurrency', () => {
  it('should work', () => {
    // TODO: add assertions for detectViewerCurrency
  });
});

describe('formatAmount', () => {
  it('should work', () => {
    // TODO: add assertions for formatAmount
  });
});
