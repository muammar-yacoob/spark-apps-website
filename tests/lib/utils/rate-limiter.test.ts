export {};
const { checkRateLimit, RATE_LIMITS } = require('../../../lib/utils/rate-limiter');

describe('checkRateLimit', () => {
  it('should work', () => {
    // TODO: add assertions for checkRateLimit
  });
});

describe('RATE_LIMITS', () => {
  it('should work', () => {
    // TODO: add assertions for RATE_LIMITS
  });
});
